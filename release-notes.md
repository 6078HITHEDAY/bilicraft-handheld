## 掌上碧玺（Telegram 式聊天）

本次更新把主界面改成会话列表 + 气泡聊天，并补上可配置的私聊规则、服务器图标缓存和消息通知。登录、协议、插件 API 不变。

- 底部导航改为「聊天 / 快捷工具 / 插件管理 / 设置」。连接与多服切换收进聊天顶栏的服务器抽屉。
- 私聊按 vanilla translate key、Player Chat target 和可编辑正则分流；默认 `/msg {player} {message}`，可在设置「聊天规则」里改模板并粘真实聊天试跑。
- 发送走乐观回显：8 秒内匹配服务器回执则去重标已发送，超时显示未确认。
- 服务器图标来自 Server List Ping 的 favicon，本地 png 缓存 6 小时，失败保留旧图。
- 后台私聊与被 @ 使用 MessagingStyle 通知，支持点进会话和直接回复。
- 在线玩家优先用已核对协议的 Player Info 包；未登记版本自动改用 `/msg ` 补全探针。

## 掌上碧玺 v1.0.6

本次更新重点增强后台挂机稳定性，并提供可选的低能耗模式。

- 增强后台保活：连接参数落盘后续跑，进程被回收后也能自动接上。
- 网络恢复后立刻重连，不再干等退避倒计时。
- 设置页新增「忽略电池优化」入口，减少系统息屏后清理后台连接的情况。
- 设置页新增「厂商后台限制」说明，列出小米、华为、荣耀（MagicOS）、OPPO、vivo、三星等系统的自启动与后台放行路径，并可一键跳转应用详情页。
- 新增「低能耗挂后台」开关：退到后台或息屏时释放唤醒锁并降低通知刷新频率；掉线概率略增，但仍会自动重连。
